Search spring pet clinic
fork it
Idu ishte bai

Now create new repository
git clone "paste repo link"  {go to code drop down to get link}
{continue same}
git init
nano new.txt
git add new.txt
git commit -m "first commit"
git push

# to create branch
git checkout -b "branch_name"

#create another file
nano new1.txt
git add new1.txt
git commit -m "Second commit to the branch"

#create another branch
git checkout -b "Second_branch_name"

git branch (list of branches)

nano new1.txt  #Give same file name but different content
git add new1.txt
git commit -m "Third commit to the branch"

git checkout
